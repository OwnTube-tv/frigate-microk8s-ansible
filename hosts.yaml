---

frigate_microk8s_servers:
  hosts:
    # a264a.mabl.online — single-node MicroK8s + Frigate NVR at site a264.
    # Public IP with router port-forward 622 -> 22; on the site-to-site VPN
    # the server is also reachable as 192.168.5.10:22.
    node-1:
      ansible_host: 87.251.30.65
      ansible_port: 622
      ansible_user: owntube_ansible
      # The server keeps WiFi as a fallback path, so it has two addresses and
      # Kubernetes cannot be left to guess which one identifies the node. Pin
      # it to the wired VPN-LAN address here, where every playbook picks it up.
      microk8s_node_ip: 192.168.5.10

...
