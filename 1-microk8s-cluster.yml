---

- hosts: frigate_microk8s_servers
  become: yes
  roles:
    - role: microk8s-cluster
      microk8s_servers_group: frigate_microk8s_servers
      microk8s_users: [ar9708, mblomdahl]
      # Single-node Frigate host: no dashboard. Precedence over role defaults.
      # helm/helm3 come pre-enabled in the MicroK8s 1.34 snap and the role is
      # enable-only — declaring them "yes" matches reality and creates the
      # helm3 snap alias, same as on the minio nodes.
      microk8s_plugins:
        cert-manager: yes
        dashboard: no
        dns: yes
        helm: yes
        helm3: yes
        hostpath-storage: yes
        ingress: yes
        metrics-server: yes
        host-access: yes
      # Server on-site at SOC-a264-s5 since 2026-08-11 with the inbound 80/443
      # path forwarded to the ingress — ACME issuance is live. Still no public
      # dashboard at all.
      create_letsencrypt_issuer: yes
      # The server has a WiFi fallback path, so Calico must not be left to pick
      # an interface on its own — confine it to the wired VPN-LAN.
      calico_ip_autodetection_method: cidr=192.168.5.0/24
      create_dashboard_ingress: no

...
